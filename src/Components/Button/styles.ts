import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  btnContainer: {
    backgroundColor: 'aqua',
    padding: 10,
    borderRadius: 5,
    marginTop: 10,
  },
  textStyle: {
    color: 'black',
    fontSize: 20,
    fontWeight: '600',
  },
});
